#include <stdio.h>
#include "system.h"


int
main(void) {
	/*FILE* fp;
	fp = fopen ("/dev/lcd", "w");
	if (fp == NULL)
	{
		fprintf(stderr, "open failed\n");
		return 0;
	}

	fprintf(fp, "This is LCD demo\n");
	fprintf(fp, "Altera HAL usage");
	fclose (fp);*/
	printf("HELLO WORLD!");
	while(1);
	return 0;
}
